function globalFunction(test: number, ...: number): (number, string)
end
function globalFunction2(test: number): (number, string)
end
local anonymousFunction = function(a: number, b, c: T, ...: string): number end
